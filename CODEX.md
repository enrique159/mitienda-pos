# Codex Entry Point

Read first `AGENTS.md`.

After that, before touching code:

- if you work in `packages/`, read `.github/references/packages.md`
- if you work in `src/`, read `.github/references/src.md`
- if you work in both, read both

The architecture references are the source of truth for the project.
